import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keeporsweep.starter',
  appName: 'KeepOrSweep',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
