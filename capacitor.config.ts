import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'keep-or-sweep',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
